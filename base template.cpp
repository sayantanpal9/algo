#include <bits/stdc++.h>
#define ll long long int
using namespace std;

mt19937_64 rng(chrono::steady_clock::now().time_since_epoch().count());
// this is a 64 bit random number generator, use rng() to use this
int main(){
ios_base::sync_with_stdio(false);
cin.tie(NULL);
cout.tie(NULL);

return 0;
}
